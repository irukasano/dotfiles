# Use the full-screen terminal dialog instead of plain tty prompts.
pinentry-program /usr/bin/pinentry-curses

# Keep decrypted credentials warm long enough for a Codex session.
default-cache-ttl 28800
max-cache-ttl 86400
