## Plan

### 2026-05-22 09:02 : yazi zip キー割当確認
- [x] `ai/tasks/lessons.md` と関連 AGENTS を確認する
- [x] `config/yazi/` の既存設定とキーマップを確認する
- [x] 既存割当の有無を整理する
- [x] 未割当時の候補キーを比較して回答する

### 2026-05-22 09:08 : yazi zip 実装 HLD
- [x] キーを `A` に確定する
- [x] 対象ルールを「選択があれば選択項目、なければホバー中ディレクトリ」に確定する
- [x] 出力先を「現在ディレクトリ」に確定する
- [x] 命名規則を「単一選択は対象名.zip、複数選択は archive-YYYYMMDD-HHMMSS.zip」に確定する
- [x] HLD を提示して合意を得る
- [x] 合意後に実装する
- [x] `yazi --debug </dev/null` で設定検証する

### 2026-05-22 09:26 : yazi scripts symlink 化
- [x] `Makefile` の `yazi-plugins` に `scripts` 配下のリンク作成を追加する
- [x] `A` のバインド先を `~/.config/yazi/scripts/zip-dir.sh` に変更する
- [x] `make -n yazi-plugins` と `yazi --debug </dev/null` で検証する

### 2026-05-22 09:29 : yazi-plugins 再実行失敗修正
- [x] `ya pkg add` 再実行時の失敗原因を確認する
- [x] `yazi-plugins` を冪等に修正する
- [x] `YUM=apt make yazi-plugins` 相当の再実行確認をする

### 2026-05-22 09:31 : 再帰 symlink 修正
- [x] 再帰 symlink の原因を確認する
- [x] `Makefile` の `ln` を symlink 置換になる形へ修正する
- [x] 誤生成された再帰 symlink を削除する
- [x] `make -n yazi-plugins -o yazi` で修正後のコマンドを確認する

## Review

### 2026-05-22 09:02 : yazi zip キー割当確認
- 原因: `config/yazi/keymap.toml` に zip アーカイブ作成用のキーバインドはなく、`config/yazi/yazi.toml` にも既存 archive を生成する opener / shell は未定義だった
- 確認内容: 既存設定には `extract` opener と archive preview はあるため、解凍と閲覧のみ設定済み
- 候補整理: 単独キーでは `A` と `C` が未使用。複合キーでは `[";", "z"]` や `[",", "z"]` のような新規プレフィクス配下が衝突しにくい
- 検証結果: `rg -n "zip|archive|compress|extract" config/yazi` と `config/yazi/keymap.toml` の確認で、圧縮作成バインド未設定を確認

### 2026-05-22 09:08 : yazi zip 実装 HLD
- 原因: zip 作成バインドが未実装だった
- 修正内容: `A` を `shell --block` 経由で `/home/sano/dotfiles/config/yazi/scripts/zip-dir.sh` に接続し、選択優先・ホバー fallback・同名中止・現在ディレクトリ外中止・非ディレクトリ中止を実装した
- 方針変更: Lua plugin 案はこの環境でローカル plugin 読み込み失敗が再現したため採用せず、組み込み shell バインドへ切り替えた
- 検証結果:
  - `yazi --debug </dev/null` で設定パース成功
  - `/tmp/yazi-zip-test` で `zip-dir.sh dir1` 実行により `dir1.zip` を生成し、`zipinfo -1` で `dir1/`, `dir1/a` を確認
  - 同名 `dir1.zip` 再生成時に `archive already exists` で中止を確認
  - 複数対象 `dir1 dir2` で `archive-YYYYMMDD-HHMMSS.zip` 生成を確認
  - ファイル指定で `not a directory` エラーを確認
  - 現在ディレクトリ外指定で `selection outside current directory is not supported` エラーを確認
  - `yazi /tmp/yazi-zip-bind` 起動後に `A` を送って `dir1.zip` が作成されることを確認

### 2026-05-22 09:26 : yazi scripts symlink 化
- 修正内容: `Makefile` の `yazi-plugins` に `~/.config/yazi/scripts` 作成と `zip-dir.sh` の symlink 作成を追加し、`A` のバインド先を `~/.config/yazi/scripts/zip-dir.sh` に変更した
- 検証結果:
  - `make -n yazi-plugins` で `mkdir -p ~/.config/yazi/scripts` と `ln -sf .../config/yazi/scripts/zip-dir.sh ~/.config/yazi/scripts/zip-dir.sh` が出力されることを確認
  - `yazi --debug </dev/null` で設定パース成功
  - 実際に `~/.config/yazi/scripts/zip-dir.sh` の symlink を作成し、`yazi /tmp/yazi-zip-bind2` で `A` を送って `dir1.zip` が作成されることを確認

### 2026-05-22 09:29 : yazi-plugins 再実行失敗修正
- 原因: `ya pkg add` は既に `package.toml` に存在する plugin を追加しようとすると non-zero exit で失敗する
- 修正内容: `package.toml` に同じ `use = ...` が無い場合だけ `ya pkg add` するよう `Makefile` を変更した
- 検証結果:
  - `YUM=apt make yazi-plugins` は `ag` 前提タスク内の `sudo apt` 認証失敗で途中停止することを確認
  - 依存インストールを飛ばした `YUM=apt make yazi-plugins -o yazi` は 0 終了し、`ya pkg add` の重複追加エラーが再発しないことを確認

### 2026-05-22 09:31 : 再帰 symlink 修正
- 原因: `ln -sf` は宛先がディレクトリ symlink のときにその中へ潜るため、`smart-tab.yazi/smart-tab.yazi` のような再帰 symlink を作ってしまう
- 修正内容: `Makefile` の `ln -sf` を `ln -sfn` に変更し、symlink 自体を置き換えるようにした
- 追加対応: 誤生成された `config/yazi/plugins/*/*.yazi` の再帰 symlink を削除した
- 検証結果:
  - `make -n yazi-plugins -o yazi` で `ln -sfn` が出力されることを確認
  - `config/yazi/plugins` 配下に余計な `*.yazi/*.yazi` が残っていないことを確認
