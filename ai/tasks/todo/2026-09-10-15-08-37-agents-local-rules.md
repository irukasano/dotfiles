## HLD

### 2026-09-10 15:08 : AGENTS のローカル規約と時刻生成
- 目的: `config/codex/AGENTS.md` に、AI 用ベースディレクトリをローカル規約で環境ごとに上書きできる仕組みと、タスク記録ファイル名に実時刻を採用する規約を追加する。
- 変更対象候補: `config/codex/AGENTS.md` のローカル規約および `ai/tasks/todo/` の運用記述。
- 非変更対象: タスク記録の既存履歴、実際の Codex 設定配置、他の dotfiles。
- 入出力: `~/.codex/AGENTS-local.md` に記した `AI_BASE_DIR` を入力として読み、タスク記録関連のパスを強制する。ベース規約の既定値は `ai` とする。`TASK_TIMESTAMP` は `date '+%Y-%m-%d-%H-%M-%S'` の出力として一度だけ定義し、記録ファイル名の日時にはその値をそのまま使う。
- 運用方法: `~/.codex/AGENTS-local.md` が存在する場合、`AGENTS.md` に追加して必ず読む。両者が矛盾する場合はローカル規約を優先する。ローカル規約では `AI_BASE_DIR` を `docs/ai` などへ上書きでき、タスク記録・レッスン・作業用ディレクトリの全パスはこれを基準にする。読み込み時の失敗時挙動を確認後に定める。
- 失敗時挙動: `~/.codex/AGENTS-local.md` が不在なら既定の `AI_BASE_DIR=ai` を使う。存在するのに読めない場合は、既定値へフォールバックせず作業を停止して理由を報告する。
- 既存機能への影響: ローカル規約が指定する環境では、タスク記録先が `ai/` から `docs/ai/` などへ変わり得る。ローカル規約がない環境では既存どおり `ai/` を使う。
- 未確定事項: なし。
- ユーザー確認が必要な項目: なし。
- ユーザー確認: 2026-09-10 に、`~/.codex/AGENTS-local.md` を存在時に必ず追加で読み、矛盾時はローカル規約を優先する方針を合意。
- ユーザー確認: 2026-09-10 に、ベース規約の `AI_BASE_DIR` は `ai`、ローカル規約で `docs/ai` などへ上書きし、タスク関連の全パスをこの基準へ統一する方針を合意。
- ユーザー確認: 2026-09-10 に、ローカル規約が存在するのに読めない場合は停止・報告し、既定値へフォールバックしない方針を合意。

### 2026-09-10 15:19 : ローカル規約のサンプル追加
- 目的: `AGENTS_local.md.example` のサンプルを追加し、環境ごとに `AI_BASE_DIR` を上書きするローカル規約の作成方法を示す。
- 変更対象: `config/codex/` 配下の新規 `AGENTS_local.md.example`。
- 非変更対象: `config/codex/AGENTS.md` の読込対象名（`AGENTS-local.md`）、既存の規約、実際のローカル規約ファイル。
- 入出力: サンプルファイルを入力としてコピー・編集し、実運用では `~/.codex/AGENTS-local.md` として利用する想定。
- 運用方法: サンプルは `config/codex/AGENTS_local.md.example` に置く。利用時は `~/.codex/AGENTS-local.md` へコピーまたはリネームする。内容は `AI_BASE_DIR` を `docs/ai` に上書きする最小例に限定する。
- 失敗時挙動: サンプルファイルは参照されず、既存の `AGENTS-local.md` 読込規約へ影響しない。
- 既存機能への影響: なし。ドキュメント用の新規ファイルのみ。
- 未確定事項: なし。
- ユーザー確認が必要な項目: なし。
- ユーザー確認: 2026-09-10 に、`config/codex/AGENTS_local.md.example` へ配置する方針を合意。
- ユーザー確認: 2026-09-10 に、実ファイル名への案内、`AI_BASE_DIR=docs/ai`、ローカル規約の優先だけを含む最小例とする方針を合意。

#### 2026-09-10 15:08 : 日時変数化の再計画
- 変更理由: タスク記録ファイル名に使う日時を、固定値や推測ではなく単一の明示的な変数から参照する仕様を追加する案が提示された。
- 変更内容: `TASK_TIMESTAMP` を `date '+%Y-%m-%d-%H-%M-%S'` の出力として一度だけ定義し、ファイル名ではその値をそのまま使う規約にする。
- 未確定事項: なし。
- ユーザー確認: 2026-09-10 に、変数名を `TASK_TIMESTAMP` として上記仕様を採用する方針を合意。

## Plan

### 2026-09-10 15:08 : AGENTS のローカル規約と時刻生成
- [x] 既存の `config/codex/AGENTS.md`、`ai/tasks` の履歴、レッスン、作業ツリー状態を確認する。
- [x] ローカル規約を読めない場合の扱いを合意する。
- [x] 合意済み HLD を更新する。
- [x] 使用可能な専用スキルがないことを確認する。
- [x] 日時変数化の仕様を合意し、HLD と Plan を更新する。
- [x] 更新した HLD と Plan を確認し、実装開始の合意を得る。
- [x] `config/codex/AGENTS.md` に `AI_BASE_DIR` の既定値、`AGENTS-local.md` の必須読み込み・優先順位・失敗時挙動を明記する。
- [x] `ai/tasks` を直接記した全規約を `AI_BASE_DIR` 基準の表記へ統一する。
- [x] `TASK_TIMESTAMP` を `date '+%Y-%m-%d-%H-%M-%S'` の出力として一度だけ定義し、その出力を変更・固定・推測せずにタスク記録名へ使うことを明記する。
- [x] 規約本文を検索し、対象となるタスク関連パスと日時フォーマットの記述が合意内容に一致することを検証する。
- [x] `git diff --check`、`git diff master --check`、変更差分を確認し、Review に結果を記録する。

### 2026-09-10 15:19 : ローカル規約のサンプル追加
- [x] 既存のローカル規約の読込対象と関連する規約を確認する。
- [x] サンプルファイルの配置を合意する。
- [x] 合意済み HLD を更新する。
- [x] 使用可能な専用スキルがないことを再確認する。
- [x] HLD と以下の Plan を確認し、実装開始の合意を得る。
- [x] 合意した場所に `AGENTS_local.md.example` を追加し、`AI_BASE_DIR` の上書き例と実ファイル名への案内を記載する。
- [x] サンプルの内容と配置、`git diff --check`、`git diff master --check` を確認し、Review に結果を記録する。

## Review

### 2026-09-10 15:08 : AGENTS のローカル規約と時刻生成
- 原因: タスク関連のパスが `ai/tasks` として規約本文へ直接埋め込まれ、環境ごとの切替点がなかった。タスク記録名の日時もフォーマット例だけで、実際に値を生成するコマンドと単一の参照元が定められていなかった。
- 修正内容: `AI_BASE_DIR` を導入して既定値を `ai` とし、すべてのタスク関連パスをこれに統一した。`~/.codex/AGENTS-local.md` が存在する場合の必須読み込み、同ファイルの優先、`AI_BASE_DIR=docs/ai` などによる上書き、存在するのに読めない場合の停止・報告を定めた。タスク記録名には `TASK_TIMESTAMP=$(date '+%Y-%m-%d-%H-%M-%S')` を一度だけ定義し、そのまま使用する規約を追加した。
- 検証: `rg` で `config/codex/AGENTS.md` のタスク関連パス、`AI_BASE_DIR`、`TASK_TIMESTAMP`、`AGENTS-local.md` を確認し、旧 `ai/tasks` の直接指定が残っていないことを確認した。`TASK_TIMESTAMP` の実行結果 `2026-09-10-15-16-50` が `YYYY-MM-DD-HH-MM-SS` 形式に一致することを確認した。
- 検証: `git diff --check`、`git diff master --check` が成功した。`config/codex/AGENTS.md` の差分を確認し、既存のユーザー変更である `.gitconfig` には触れていない。

### 2026-09-10 15:19 : ローカル規約のサンプル追加
- 原因: `AGENTS-local.md` はローカル環境向けの読込規約として追加されたが、作成時に使える最小の雛形がなかった。
- 修正内容: `config/codex/AGENTS_local.md.example` を追加した。実際の読込対象である `~/.codex/AGENTS-local.md` へのコピーまたはリネームを案内し、`AI_BASE_DIR` を `docs/ai` へ上書きする例とローカル規約の優先だけを記載した。
- 検証: サンプル本文と `config/codex/AGENTS.md` を検索し、実ファイル名、`AI_BASE_DIR`、およびローカル規約の優先が一貫していることを確認した。`git diff --check` と `git diff master --check` が成功した。既存のユーザー変更 `.gitconfig` には触れていない。
