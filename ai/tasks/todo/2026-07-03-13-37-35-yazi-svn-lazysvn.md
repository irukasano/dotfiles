## HLD

### 2026-07-03 13:37 : yazi svn lazysvn
- 目的: `yazi` の `S` プレフィクス配下に `l` を追加し、`lazysvn` を起動できるようにする
- 変更対象: `config/yazi/keymap.toml` の `SVN` 関連キーマップ定義
- 非変更対象: 既存の `svn` plugin 実装、他の `yazi` キーバインド、`lazysvn` 本体の設定
- 入出力:
  - 入力: `yazi` マネージャ画面で `S` → `l`
  - 出力: 合意した方法で `lazysvn` が起動する
- 運用方法: 既存の `S a/c/s/u` と同じ `SVN` プレフィクス配下に追加する
- 失敗時挙動: `shell "lazysvn" --block` に委譲し、`lazysvn` 未インストールや起動失敗時はシェル実行エラーとして扱う
- 既存機能への影響: `S` プレフィクス配下に 1 キー追加するのみで、既存 `a/c/s/u` は維持する想定
- 未確定事項: なし
- ユーザー確認が必要な項目: なし

## Plan

### 2026-07-03 13:37 : yazi svn lazysvn
- [x] HLD の未確定事項をユーザー確認で確定する
- [x] 合意した内容で `config/yazi/keymap.toml` を更新する
- [x] `yazi --debug </dev/null` で設定パースを検証する

## Review

### 2026-07-03 13:37 : yazi svn lazysvn
- 原因: `S` プレフィクス配下の `SVN` 操作には `a/c/s/u` しかなく、`lazysvn` を起動するキーバインドが未設定だった
- 修正内容: `config/yazi/keymap.toml` に `[[mgr.prepend_keymap]]` を追加し、`["S", "l"]` で `shell "lazysvn" --block` を実行するようにした
- 検証結果:
  - `yazi --debug </dev/null` が 0 終了し、更新後の `keymap.toml` を含めて設定パース成功を確認
  - `command -v lazysvn` で `/home/sano/.local/bin/lazysvn` を確認
